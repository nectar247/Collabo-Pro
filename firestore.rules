rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Homepage Cache - Public read access for performance
    match /homepageCache/{document=**} {
      allow read: if true;
      allow write: if false; // Only Cloud Functions can write
    }

    // Deals - Public read, admin write
    match /deals_fresh/{dealId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Allow aggregation queries on deals_fresh for everyone
    match /deals_fresh {
      allow read: if true;
    }

    // Categories - Public read, admin write
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Brands - Public read, admin write
    match /brands/{brandId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Search History - Public read/write (for analytics)
    match /search_history/{searchId} {
      allow read: if true;
      allow create: if true;
      allow update: if true;
      allow delete: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // User Profiles - User can read/write their own, admins can read all
    match /profiles/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Allow aggregation queries on profiles for admins
    match /profiles {
      allow read: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Settings - Public read, admin write
    match /settings/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Dynamic Links - Public read, admin write
    match /dynamicLinks/{linkId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // FAQs - Public read, admin write
    match /faqs/{faqId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Blog Posts - Public read, admin write
    match /blog_posts/{postId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Media Files - Public read, admin write
    match /media_files/{fileId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // About Content - Public read, admin write
    match /about/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Content sections - Public read, admin write
    match /content/{contentId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Transactions - Admin only (for analytics)
    match /transactions/{transactionId} {
      allow read: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Sessions - Admin only (for analytics)
    match /sessions/{sessionId} {
      allow read: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/profiles/$(request.auth.uid)).data.isAdmin == true;
    }

    // Saved Deals - Users can read/write their own saved deals
    match /deals_saved/{dealId} {
      allow read: if request.auth != null && resource.data.profileId == request.auth.uid;
      allow create: if request.auth != null && request.resource.data.profileId == request.auth.uid;
      allow update: if request.auth != null && resource.data.profileId == request.auth.uid;
      allow delete: if request.auth != null && resource.data.profileId == request.auth.uid;
    }

    // Default deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
